{
  "name": "smart-student-analytics",
  "version": "1.0.0",
  "description": "Root package for Render deployment",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
